program TestEnum;

type
  Day == (Mon, Tue, Wed, Thu, Fri);

var
  d: Day;
  x : string;
begin
  d := Tue;
  if d == Tue then
    writeln(1)
  else
    writeln(0);

  d := Fri;
  if d == Fri then
    writeln(5)
  else
    writeln(0);

  readln(x);
  writeln(x);
end.