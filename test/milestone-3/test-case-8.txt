program TC8;
var
  a, b, c: Integer;
  x: Real;
  flag: Boolean;
begin
  a := 10;
  b := 3;
  c := a + b;
  c := a - b;
  c := a * b;
  c := a div b;
  c := a mod b;
  x := a / b;
  flag := a > b;
  flag := a < b;
  flag := a >= b;
  flag := a <= b;
  flag := a <> b;
  if flag then
    c := a
  else
    c := b;
end.
