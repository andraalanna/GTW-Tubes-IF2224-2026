(*Tes case untuk uji ketahanan program lexer*)

Program robustness;

var
 
{uji string dan char}
    q := '';
    s := '''';
    t := 'it''s';
    v := 'string\n biasa';
    w := 'belum ditutup
    {input x := '''}
    x := ''';

(* comment edge case *)
    { komentar tidak ditutup
    (* komentar bintang tidak ditutup

(* comment dengan karakter spesial di dalamnya *)
    { komentar dengan } di tengah lanjutan }
    (* komentar dengan *) di tengah lanjutan *)

end.
